# Environment variables template for PadelUp Backend
DEBUG=True
SECRET_KEY=django-insecure-+avgk0u*8vw*r^5==mag#n*jip8*yh(g$!hm*2%@teo(l*)-z6
ALLOWED_HOSTS=localhost,127.0.0.1

# Email configuration
# For local testing, you can use the Console backend (emails print to the terminal logs)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# Or configure your SMTP (OVH SMTP shown below as an example)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=ssl0.ovh.net
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_USE_SSL=False
# EMAIL_HOST_USER=contact@padel-up.io
# EMAIL_HOST_PASSWORD=your-ovh-mailbox-password
DEFAULT_FROM_EMAIL=noreply@padelup.com
